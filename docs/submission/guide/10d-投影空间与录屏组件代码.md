# 10d · 投影空间与录屏组件代码

[← 10 答辩代码讲解](./10-答辩代码讲解.md) · [← 主索引](../NyxViz_零基础完全解读.md)

---

## 本章是干什么的

[第 10 章](./10-答辩代码讲解.md) 讲全局联动与任务管线；本章逐文件讲解 **XY 投影 / 空间验证** 与 **录屏页 11 场景组件**，含真实代码摘录与 30 秒答辩话术。

---

## 空间与投影

## BandPreviewCanvas.tsx

**路径**：[`src/spatial/BandPreviewCanvas.tsx`](../../../src/spatial/BandPreviewCanvas.tsx)

**职责**：将 128×128 最大密度投影画到 canvas；刷选区间内像素用 BRUSH_HIGHLIGHT_RGB 高亮，非刷选区用 cosmic LUT。导出 `drawProjectionToCanvas` 供录屏四格预览复用。

**为什么选这段**：展示刷选高亮与 LUT 上色核心循环。

```42:62:src/spatial/BandPreviewCanvas.tsx
  for (let j = 0; j < GRID_SIZE; j++) {
    for (let i = 0; i < GRID_SIZE; i++) {
      const v = proj[i + j * GRID_SIZE]!;
      const inBrush =
        brushRange !== null && v >= brushRange.min && v <= brushRange.max;
      const t = densityToUnit(v, domainMin, domainMax, true);
      const li = Math.min(255, Math.round(t * 255)) * 4;
      const o = ((GRID_SIZE - 1 - j) * GRID_SIZE + i) * 4;
      if (inBrush) {
        px[o] = Math.round(hr * 255);
        px[o + 1] = Math.round(hg * 255);
        px[o + 2] = Math.round(hb * 255);
        px[o + 3] = 255;
      } else {
        px[o] = COSMIC_LUT[li]!;
        px[o + 1] = COSMIC_LUT[li + 1]!;
        px[o + 2] = COSMIC_LUT[li + 2]!;
        px[o + 3] = 255;
      }
    }
  }
```

**输入 / 输出**：输入 `projection`（128² Float32）、`brushRange`、`domainMin/Max` → 输出带高亮的 canvas 像素。

**答辩 30 秒**：录屏右栏四格不重复算投影，只把共享投影按不同密度区间着色；高亮色与体渲染、直方图刷选共用 store 里的 brushRange，实现统计—空间一致。

## DensityProjection.tsx

**路径**：[`src/spatial/DensityProjection.tsx`](../../../src/spatial/DensityProjection.tsx)

**职责**：从当前步 128³ 体数据异步计算最大密度投影（Worker），idle 后绘制到 canvas；支持 axis 与 compact 模式。

**为什么选这段**：展示异步投影 + 防抖调度。

```100:124:src/spatial/DensityProjection.tsx
  useEffect(() => {
    const canvas = canvasRef.current;
    if (!canvas) return;

    let cancelled = false;
    let idleId: number | undefined;

    const timer = window.setTimeout(() => {
      const run = () => {
        if (cancelled) return;
        void computeMaxProjectionAsync(data, axis).then((proj) => {
          if (cancelled) return;
          const w = canvas.clientWidth || 400;
          const h = canvas.clientHeight || 280;
          const dpr = Math.min(window.devicePixelRatio || 1, 1.5);
          canvas.width = w * dpr;
          canvas.height = h * dpr;

          const ctx = canvas.getContext('2d');
          if (!ctx) return;
          ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
          ctx.clearRect(0, 0, w, h);
          drawProjection(ctx, proj, domainMin, domainMax, brushRange, w, h, compact);
        });
      };
```

**输入 / 输出**：输入 `data`、`brushRange`、`axis`、`domainMin/Max` → 输出 2D canvas 视图。

**答辩 30 秒**：完整投影链路：体数据经 computeMaxProjectionAsync 沿轴取 max，主线程用 log 域 cosmic 色标绘制；280ms 防抖和 requestIdleCallback 避免拖慢 vtk 体渲染。

## BrushedPoints.tsx

**路径**：[`src/spatial/BrushedPoints.tsx`](../../../src/spatial/BrushedPoints.tsx)

**职责**：用 vtk.js 将刷选体素坐标渲染为 3D 散点，辅助验证刷选结果的空间聚集性。

**为什么选这段**：展示体素坐标转 vtk 点云。

```83:108:src/spatial/BrushedPoints.tsx
  useEffect(() => {
    const ctx = contextRef.current;
    if (!ctx || points.length === 0) return;

    const coords = new Float32Array(points.length * 3);
    for (let i = 0; i < points.length; i++) {
      const p = points[i]!;
      coords[i * 3] = (p.x + 0.5) * SPACING;
      coords[i * 3 + 1] = (p.y + 0.5) * SPACING;
      coords[i * 3 + 2] = (p.z + 0.5) * SPACING;
    }

    const n = points.length;
    const verts = new Uint32Array(n * 2);
    for (let i = 0; i < n; i++) {
      verts[i * 2] = 1;
      verts[i * 2 + 1] = i;
    }

    const vtkPts = ctx.polyData.getPoints();
    vtkPts.setData(coords, 3);
    ctx.polyData.getVerts().setData(verts, 1);
    ctx.polyData.modified();

    ctx.fullScreenRenderer.getRenderWindow().render();
  }, [points]);
```

**输入 / 输出**：输入 `points: BrushedVoxel[]` → 输出 vtk 全屏点云视图。

**答辩 30 秒**：刷选不只画 2D 投影，还可把 Worker 采样体素转成物理坐标点云，直观看 Top 1% 是否沿纤维聚集。

## TriaxialSlices.tsx

**路径**：[`src/spatial/TriaxialSlices.tsx`](../../../src/spatial/TriaxialSlices.tsx)

**职责**：在体数据中心切 XY / XZ / YZ 三平面，用 cosmic LUT 绘制 128×128 切片缩略图。

**为什么选这段**：展示三平面采样与 LUT 映射。

```11:46:src/spatial/TriaxialSlices.tsx
function fillPlane(
  data: Float32Array,
  plane: PlaneId,
  domainMin: number,
  domainMax: number,
): Uint8ClampedArray {
  const rgba = new Uint8ClampedArray(GRID_SIZE * GRID_SIZE * 4);
  for (let a = 0; a < GRID_SIZE; a++) {
    for (let b = 0; b < GRID_SIZE; b++) {
      let x: number;
      let y: number;
      let z: number;
      if (plane === 'xy') {
        x = a;
        y = b;
        z = MID;
      } else if (plane === 'xz') {
        x = a;
        y = MID;
        z = b;
      } else {
        x = MID;
        y = a;
        z = b;
      }
      const v = getVoxel(data, x, y, z);
      const u = densityToUnit(v, domainMin, domainMax, true);
      const li = Math.min(255, Math.floor(u * 255)) * 4;
      const o = (a + b * GRID_SIZE) * 4;
      rgba[o] = LUT[li]!;
      rgba[o + 1] = LUT[li + 1]!;
      rgba[o + 2] = LUT[li + 2]!;
      rgba[o + 3] = 255;
    }
  }
  return rgba;
```

**输入 / 输出**：输入 `data`、`domainMin/Max` → 输出三张 canvas 切片；`data=null` 时显示占位文案。

**答辩 30 秒**：三轴切片帮助评委区分「投影是沿轴压缩、切片是单平面剖面」；色标与体渲染全局 p01–p99 一致。

## useSharedProjection.ts

**路径**：[`src/hooks/useSharedProjection.ts`](../../../src/hooks/useSharedProjection.ts)

**职责**：React Hook：320ms 防抖后调用 computeMaxProjectionAsync，多组件共享同一份投影缓存。

**为什么选这段**：展示投影复用与取消逻辑。

```5:31:src/hooks/useSharedProjection.ts
export function useSharedProjection(
  data: Float32Array | null | undefined,
  axis: ProjectionAxis = 'xy',
): Float32Array | null {
  const [projection, setProjection] = useState<Float32Array | null>(null);

  useEffect(() => {
    if (!data) {
      setProjection(null);
      return;
    }

    let cancelled = false;
    const timer = window.setTimeout(() => {
      void computeMaxProjectionAsync(data, axis).then((proj) => {
        if (!cancelled) setProjection(proj);
      });
    }, 320);

    return () => {
      cancelled = true;
      window.clearTimeout(timer);
    };
  }, [data, axis]);

  return projection;
}
```

**输入 / 输出**：输入 `data`、`axis` → 输出 `Float32Array | null` 投影状态。

**答辩 30 秒**：VideoBrushPreviews 四格预览只算一次 XY 投影，各格用不同 brushRange 调用 BandPreviewCanvas 着色，性能友好。

## 录屏壳层

## VideoApp.tsx

**路径**：[`src/dashboard/VideoApp.tsx`](../../../src/dashboard/VideoApp.tsx)

**职责**：录屏页 React 入口：加载 timeline 与 videoStats，挂 useNyxTimestep 拉体数据，渲染 VideoDashboard。

**为什么选这段**：展示 video.html 启动链。

```17:59:src/dashboard/VideoApp.tsx
export function VideoApp() {
  const [timeline, setTimeline] = useState<TimelineData | null>(null);
  const [videoStats, setVideoStats] = useState<VideoStatsBundle>(EMPTY_STATS);
  const [initError, setInitError] = useState<string | null>(null);

  const densityData = useAppStore((s) => s.densityData);
  const loading = useAppStore((s) => s.loading);
  const error = useAppStore((s) => s.error);

  useNyxTimestep();
  useTimelineVolumePreload(!isStaticFiguresOnly());

  useEffect(() => {
    loadTimelineStats()
      .then(setTimeline)
      .catch((e: unknown) =>
        setInitError(e instanceof Error ? e.message : String(e)),
      );
    loadVideoStats().then(setVideoStats);
  }, []);

  if (initError) {
    return (
      <div className="app-error">
        <h1>Nyx 录屏三栏</h1>
        <p>{initError}</p>
      </div>
    );
  }

  if (!timeline) {
    return <div className="app-loading">加载统计数据…</div>;
  }

  return (
    <VideoDashboard
      timeline={timeline}
      densityData={densityData}
      loading={loading}
      error={error}
      videoStats={videoStats}
    />
  );
```

**输入 / 输出**：输入 URL → 输出完整录屏仪表盘；依赖 public/stats/*.json 与 Nyx/*.dat。

**答辩 30 秒**：video.html 根组件先拉统计 JSON，再靠全局 store 按步 fetch .dat；与 app 页共用 nyxLoader 和 store，保证数字一致。

## VideoDashboard.tsx

**路径**：[`src/dashboard/VideoDashboard.tsx`](../../../src/dashboard/VideoDashboard.tsx)

**职责**：录屏总控：useVideoScene 管场景、useDashboardInteraction 管刷选换步，按 sceneMeta 分发 props 给 VideoSceneLayout。

**为什么选这段**：展示 videoMode 交互与场景入场刷选。

```91:160:src/dashboard/VideoDashboard.tsx
  const ix = useDashboardInteraction(timeline, densityData, loading, {
    defaultHighQuality: false,
    progressiveQuality: true,
    keepVolumeAlive: false,
    instantVolume: false,
    videoMode: true,
    idleBoostDelayMs: recordMode ? 350 : 1200,
  });

  const {
    timestep,
    sliderStep,
    sliderDragging,
    setSliderStep,
    setSliderDragging,
    volumeReady,
    onVolumeRendered,
    onVolumeCameraActivity,
    brushRange,
    tfParams,
    stats,
    dataMin,
    dataMax,
    volumeQuality,
    qualityPhase,
    highlight,
    applyTop1,
    applyBottom1,
    applyFilament,
    clearBrush,
    commitTimestep,
    selectTimestep,
    volumeRatio,
    activePreset,
    timestepCount,
  } = ix;

  const visualProfile = useVolumeVisualProfile(timeline, timestep, sceneId);
  const volumeHighlightMin = highlight.highlightMin ?? visualProfile.highlightMin;
  const volumeHighlightMax = highlight.highlightMax ?? visualProfile.highlightMax;

  useVideoPosterCapture({
    enabled: posterCapture,
    recordMode,
    sceneId,
    loading,
    volumeReady,
  });

  useEffect(() => {
    setTimestep(sceneMeta.defaultTimestep);
    setSliderStep(sceneMeta.defaultTimestep);
    sceneBrushApplied.current = false;
  }, [sceneMeta.defaultTimestep, sceneMeta.id, setTimestep, setSliderStep]);

  useEffect(() => {
    if (!stats || sceneBrushApplied.current) return;
    sceneBrushApplied.current = true;
    if (sceneMeta.brushPreset === 'top') applyTop1();
    else if (sceneMeta.brushPreset === 'bottom') applyBottom1();
    else if (sceneMeta.brushPreset === 'filament') applyFilament();
    else clearBrush();
  }, [
    stats,
    sceneMeta.brushPreset,
    applyTop1,
    applyBottom1,
    applyFilament,
    clearBrush,
  ]);
```

**输入 / 输出**：输入 timeline、densityData、videoStats → 输出顶栏+场景布局+CosmicBackdrop。

**答辩 30 秒**：录屏大脑：切场景时重置默认时间步并按 sceneRegistry 自动 applyTop1 等预设刷选；videoMode 降低体渲染优先级保证流畅。

## VideoSceneLayout.tsx

**路径**：[`src/dashboard/VideoSceneLayout.tsx`](../../../src/dashboard/VideoSceneLayout.tsx)

**职责**：按 sceneId 分支：专用场景（空间统计/void/验证）或标准左中右三栏；中栏体渲染用 PersistentVolumePane。

**为什么选这段**：展示 11 场景布局分支。

```96:128:src/dashboard/VideoSceneLayout.tsx
  const body = !hasBody ? null : sceneId === 'task2-spatial' ? (
    <div className="vd-body vd-body--spatial">
      <VideoSpatialPanel validation={videoStats.validationExtended} />
    </div>
  ) : sceneId === 'task2-void' ? (
    <VideoVoidScene validation={videoStats.validationExtended} />
  ) : sceneId === 'task4-validate' ? (
    <div className="vd-body vd-body--validate">
      <VideoValidateFigureColumn />
      {showRight && (
        <VideoRightColumn
          sceneId={sceneId}
          videoStats={videoStats}
          timeline={props.timeline}
          densityData={props.densityData}
          loading={props.loading}
          stats={props.stats}
          timestep={props.timestep}
          dataMin={props.dataMin}
          dataMax={props.dataMax}
          volumeReady={props.volumeReady}
          brushRange={props.brushRange}
          volumeRatio={props.volumeRatio}
          onTop1={props.onTop1}
          onBottom1={props.onBottom1}
          onFilament={props.onFilament}
          onClear={props.onClear}
          activePreset={props.activePreset}
          histogramSizeOpts={props.histogramSizeOpts}
          onSelectTimestep={props.onSelectTimestep}
        />
      )}
    </div>
```

**输入 / 输出**：输入 VideoSceneLayoutProps → 输出左/中/右/底栏 DOM 树。

**答辩 30 秒**：11 个场景不是 11 个页面，而是同一壳根据 sceneRegistry 开关栏位；task2-spatial 走全宽配图，task4-brush 走右栏直方图+投影。

## VideoDashboardLayout.tsx

**路径**：[`src/dashboard/VideoDashboardLayout.tsx`](../../../src/dashboard/VideoDashboardLayout.tsx)

**职责**：早期单场景固定三栏（左趋势、中体渲染、右刷选）；现由 VideoSceneLayout 取代，仍作 intro 原型对照。

**为什么选这段**：展示经典三栏组合。

```91:164:src/dashboard/VideoDashboardLayout.tsx
  return (
    <>
      <div className="vd-body">
        <aside className="vd-panel vd-panel-left">
          <header className="vd-panel-head">
            <h2>时序密度分布演化</h2>
          </header>
          <div className="vd-panel-hist">
            <HistogramOverlay timeline={timeline} sizeOpts={histOverlaySize} />
          </div>
          {stats && <VideoKpiStrip timeline={timeline} stats={stats} />}
          <div className="vd-mini-trends">
            <PosterTrendChart
              timeline={timeline}
              title="σ(t)"
              badge={`+${m.sigmaPct.toFixed(1)}%`}
              color="#00d4ff"
              fill="rgba(0, 212, 255, 0.12)"
              metric="std"
              sizeOpts={VIDEO_MINI_TREND}
              compact
            />
            <PosterTrendChart
              timeline={timeline}
              title="Top 1% Δ"
              badge={tailBadge}
              color="#ff9500"
              fill="rgba(255, 149, 0, 0.12)"
              metric="tailPct"
              sizeOpts={VIDEO_MINI_TREND}
              compact
            />
            <PosterTrendChart
              timeline={timeline}
              title="p99−p01"
              badge={`+${m.spanPct.toFixed(1)}%`}
              color="#9b8cf8"
              fill="rgba(155, 140, 248, 0.12)"
              metric="span"
              sizeOpts={VIDEO_MINI_TREND}
              compact
            />
          </div>
        </aside>

        <main className="vd-panel vd-panel-center">
          <div className="vd-vtk-frame">
            <div className="vd-vtk-canvas-wrap">
              <LoadingOverlay visible={loading} label={`t=${timestep}`} />
              {densityData ? (
                <Suspense fallback={<VtkFallback />}>
                  <VolumeScene
                    data={densityData}
                    timestep={timestep}
                    dataMin={dataMin}
                    dataMax={dataMax}
                    tfParams={tfParams}
                    quality={volumeQuality}
                    renderActive
                    cameraZoom={VIDEO_CAMERA_ZOOM}
                    highlightMin={highlightMin}
                    highlightMax={highlightMax}
                    onRendered={onVolumeRendered}
                    visualStyle="cinematic"
                    className="vd-vtk-panel"
                  />
                </Suspense>
              ) : (
                <VtkFallback />
              )}
            </div>
            <DensityColorLegend min={dataMin} max={dataMax} />
          </div>
        </main>
```

**输入 / 输出**：输入时间步、刷选回调、体数据等 → 输出固定三栏 + VideoFindingsStrip 底栏。

**答辩 30 秒**：这是开篇三栏原型：左 HistogramOverlay+KPI、中 VolumeScene、右 DensityHistogram+VideoBrushPreviews；多场景版拆成 Left/RightColumn 按场景换面板。

## sceneRegistry.ts

**路径**：[`src/video/sceneRegistry.ts`](../../../src/video/sceneRegistry.ts)

**职责**：11 段录屏场景元数据：默认时间步、刷选预设、左中右栏显隐、布局 CSS 变量、配图列表。

**为什么选这段**：展示场景配置表结构。

```80:98:src/video/sceneRegistry.ts
export const VIDEO_SCENES: VideoSceneMeta[] = [
  {
    id: 'intro',
    title: '开篇 · 三栏总览',
    narrationIndex: 0,
    defaultTimestep: 99,
    brushPreset: null,
    layout: 'full',
    showLeft: true,
    showCenter: true,
    showRight: true,
    showFindings: true,
    layoutTokens: {
      findingsHeight: 252,
      recordFindingsHeight: 268,
      bodyColumns: 'default',
      centerMinHeight: 420,
    },
    content: {
```

**输入 / 输出**：输入 sceneId → 输出 VideoSceneMeta；sceneLayoutStyle 生成 CSS 变量。

**答辩 30 秒**：改录屏默认值、某场景是否显示体渲染或发现卡，都看 sceneRegistry；与 06 录屏手册段落一一对应。

## useVideoScene.ts

**路径**：[`src/video/useVideoScene.ts`](../../../src/video/useVideoScene.ts)

**职责**：解析 URL ?scene= 与 ?record=1，管理当前场景 id 与 history 导航；录屏模式切场景用整页跳转保 OBS 分段干净。

**为什么选这段**：展示 URL 与场景状态同步。

```19:62:src/video/useVideoScene.ts
export function useVideoScene() {
  const recordMode = useMemo(() => isVideoRecordMode(), []);
  const [sceneId, setSceneIdState] = useState<VideoSceneId>(() => readSceneFromUrl());
  const sceneMeta = useMemo(() => getSceneMeta(sceneId), [sceneId]);

  useEffect(() => {
    if (recordMode) return;
    const onPopState = () => setSceneIdState(readSceneFromUrl());
    window.addEventListener('popstate', onPopState);
    return () => window.removeEventListener('popstate', onPopState);
  }, [recordMode]);

  const setScene = useCallback(
    (id: VideoSceneId) => {
      if (recordMode) {
        const params = new URLSearchParams(window.location.search);
        params.set('record', '1');
        if (id === 'intro') {
          params.delete('scene');
        } else {
          params.set('scene', id);
        }
        const q = params.toString();
        window.location.href = q
          ? `${window.location.pathname}?${q}`
          : window.location.pathname;
        return;
      }
      const params = new URLSearchParams(window.location.search);
      if (id === 'intro') {
        params.delete('scene');
      } else {
        params.set('scene', id);
      }
      const q = params.toString();
      const url = q ? `/video.html?${q}` : '/video.html';
      window.history.replaceState(null, '', url);
      setSceneIdState(id);
    },
    [recordMode],
  );

  return { sceneId, sceneMeta, setScene, recordMode };
}
```

**输入 / 输出**：输入 window.location → 输出 sceneId、sceneMeta、setScene、recordMode。

**答辩 30 秒**：彩排预览用 history.replaceState 快切；正式录屏用带 record=1 的独立 URL，每段对应一个 scene 参数。

## narrationLabels.ts

**路径**：[`src/video/narrationLabels.ts`](../../../src/video/narrationLabels.ts)

**职责**：旁白/TTS 统一用语：σ、尾段体积、p99−p01 等标签，避免 KPI 卡与口述稿用词不一致。

**为什么选这段**：展示旁白标签常量。

```1:20:src/video/narrationLabels.ts
/** 旁白 TTS 统一用语标签 */
export const NARRATION_LABELS = {
  sigma: '密度标准差 σ',
  tailAbove: '≥p99 体积占比',
  tailBelow: 'Bottom 1% 体积占比',
  span: 'p99−p01 分位跨度',
  mean: '平均密度 μ',
  introHeadline:
    '左栏百步直方图与趋势 · 中栏体渲染 · 右栏刷选与投影',
  precomputeHint: '请运行 npm run precompute 生成 public/stats/',
} as const;

export type KpiDisplayMode = 'sigma' | 'variance';

export interface KpiItem {
  label: string;
  value: string;
  badge?: string;
  tone: 'gold' | 'orange' | 'cyan' | 'blue';
}
```

**输入 / 输出**：无运行时 I/O；被 VideoKpiStrip、VideoHistMethodStrip 等 import。

**答辩 30 秒**：答辩口述和屏幕 KPI 用同一套 NARRATION_LABELS，评委对照 Word 和录屏不会看到「σ² 和 σ 混用」。

## 录屏 layout

## types.ts

**路径**：[`src/dashboard/video-scenes/layout/types.ts`](../../../src/dashboard/video-scenes/layout/types.ts)

**职责**：VideoSceneLayoutProps 与 VolumePaneProps 类型契约，统一左中右栏与体渲染 pane 的 props 形状。

**为什么选这段**：展示布局 props 接口。

```10:49:src/dashboard/video-scenes/layout/types.ts
export interface VideoSceneLayoutProps {
  sceneId: VideoSceneId;
  sceneMeta: VideoSceneMeta;
  videoStats: VideoStatsBundle;
  timeline: TimelineData;
  densityData: Float32Array | null;
  loading: boolean;
  timestep: number;
  stats: TimelineData['timesteps'][0] | undefined;
  dataMin: number;
  dataMax: number;
  volumeDataMin?: number;
  volumeDataMax?: number;
  tfParams: TfParams;
  volumeQuality: VolumeQuality;
  qualityPhase: 'draft' | 'final';
  volumeReady: boolean;
  onVolumeRendered: () => void;
  onVolumeCameraActivity?: () => void;
  brushRange: BrushRange | null;
  highlightMin?: number;
  highlightMax?: number;
  volumeRatio: string | null;
  onTop1: () => void;
  onBottom1: () => void;
  onFilament: () => void;
  onClear: () => void;
  activePreset: BrushPresetId | null;
  histOverlaySize?: ChartSizeOptions;
  histogramSizeOpts?: ChartSizeOptions;
  keepVolumeAlive: boolean;
  volumeEverMounted: boolean;
  onVolumeMounted: () => void;
  onSelectTimestep: (t: number) => void;
  volumeInteractive?: boolean;
  volumeFocusOnClick?: boolean;
  volumeCameraZoom?: number;
  /** 录屏浏览：场景条嵌入 main-stack 顶栏，避免额外 grid 行挤压布局 */
  recordBrowse?: ReactNode;
}
```

**输入 / 输出**：TypeScript 类型；VideoDashboard → VideoSceneLayout → 各 Column 的数据流定义。

**答辩 30 秒**：答辩展示「数据怎么流」时打开此文件：一个扁平 props 从 Dashboard 贯穿三栏，避免各栏各自读 store 造成不同步。

## shared.ts

**路径**：[`src/dashboard/video-scenes/layout/shared.ts`](../../../src/dashboard/video-scenes/layout/shared.ts)

**职责**：共享 VIDEO_MINI_TREND 图表尺寸与 computeTailBadge 尾段体积 badge 算法。

**为什么选这段**：展示左栏迷你趋势共用工具。

```5:19:src/dashboard/video-scenes/layout/shared.ts
export const VIDEO_MINI_TREND = {
  minHeight: 86,
  maxHeight: 92,
  aspect: 1.55,
  fillContainer: true,
  videoReadable: true,
} as const;

export function computeTailBadge(timeline: VideoSceneLayoutProps['timeline']): string {
  const tailSeries = timeline.timesteps.map((s) => s.tailMassAboveP99 * 100);
  const tail0 = tailSeries[0]!;
  const tailPeak = Math.max(...tailSeries);
  const tailRel = tail0 > 1e-6 ? ((tailPeak - tail0) / tail0) * 100 : 0;
  return tailRel >= 0.0005 ? `+${tailRel.toFixed(tailRel < 0.1 ? 3 : 1)}%` : '≈0%';
}
```

**输入 / 输出**：输入 timeline → 输出尾段 badge 字符串；输出 ChartSizeOptions 常量。

**答辩 30 秒**：左栏三条 PosterTrendChart 共用尺寸 token 和 tail badge 算法，录屏字号可读且与 KPI 条数字一致。

## VideoLeftColumn.tsx

**路径**：[`src/dashboard/video-scenes/layout/VideoLeftColumn.tsx`](../../../src/dashboard/video-scenes/layout/VideoLeftColumn.tsx)

**职责**：按 sceneId 渲染左栏：传递函数表、演化量化、案例卡、直方图方法条或默认直方图+KPI+趋势。

**为什么选这段**：展示左栏场景分支。

```28:77:src/dashboard/video-scenes/layout/VideoLeftColumn.tsx
  return (
    <aside className="vd-panel vd-panel-left">
      {sceneId === 'task1-tf' && (
        <VideoRenderSpecPanel renderSpec={videoStats.renderSpec} timeline={timeline} />
      )}

      {sceneId === 'task2-evolution' && (
        <>
          <header className="vd-panel-head">
            <h2>时序密度分布演化</h2>
          </header>
          <div className="vd-panel-hist vd-panel-hist--tall">
            <HistogramOverlay timeline={timeline} sizeOpts={histOverlaySize} />
          </div>
          <VideoEvolutionPanel timeline={timeline} />
          {stats && <VideoKpiStrip timeline={timeline} stats={stats} showSigma />}
          <div className="vd-mini-trends">
            <PosterTrendChart
              timeline={timeline}
              title="σ(t)"
              badge={`+${m.sigmaPct.toFixed(1)}%`}
              color="#00d4ff"
              fill="rgba(0, 212, 255, 0.12)"
              metric="std"
              sizeOpts={VIDEO_MINI_TREND}
              compact
            />
            <PosterTrendChart
              timeline={timeline}
              title={NARRATION_LABELS.tailAbove}
              badge={tailBadge}
              color="#ff9500"
              fill="rgba(255, 149, 0, 0.12)"
              metric="tailPct"
              sizeOpts={VIDEO_MINI_TREND}
              compact
            />
            <PosterTrendChart
              timeline={timeline}
              title={NARRATION_LABELS.span}
              badge={`+${m.spanPct.toFixed(1)}%`}
              color="#9b8cf8"
              fill="rgba(155, 140, 248, 0.12)"
              metric="span"
              sizeOpts={VIDEO_MINI_TREND}
              compact
            />
          </div>
        </>
      )}
```

**输入 / 输出**：输入 sceneId、videoStats、timeline、stats → 输出左栏 React 树。

**答辩 30 秒**：左栏是叙事统计区：task1-tf 看 renderSpec，task2-evolution 看演化量化+趋势，数据源全是 timeline.json 不现场算百步。

## VideoCenterColumn.tsx

**路径**：[`src/dashboard/video-scenes/layout/VideoCenterColumn.tsx`](../../../src/dashboard/video-scenes/layout/VideoCenterColumn.tsx)

**职责**：封装 VolumeScene 与静态 PNG 回退；PersistentVolumePane 支持离屏保活减少 vtk 重建。

**为什么选这段**：展示体渲染 pane 与保活。

```89:118:src/dashboard/video-scenes/layout/VideoCenterColumn.tsx
        {densityData ? (
          <VolumeScene
            data={densityData}
            timestep={timestep}
            dataMin={dataMin}
            dataMax={dataMax}
            tfParams={tfParams}
            quality={volumeQuality}
            renderActive={renderActive}
            cameraZoom={cameraZoom}
            showOrientation={false}
            highlightMin={highlightMin}
            highlightMax={highlightMax}
            onRendered={onVolumeRendered}
            onCameraActivity={onVolumeCameraActivity}
            interactiveCamera={interactiveCamera}
            adaptivePrecisionZoom={interactiveCamera || focusOnClick}
            focusOnClick={focusOnClick}
            focusDensityThreshold={focusDensityThreshold}
            performanceMode={performanceMode}
            visualStyle={visualStyle}
            onFocusChange={setFocused}
            className="vd-vtk-panel"
          />
        ) : (
          <div className="vd-vtk-fallback">加载密度场…</div>
        )}
      </div>
      <DensityColorLegend min={dataMin} max={dataMax} />
    </div>
```

**输入 / 输出**：输入 VolumePaneProps → 输出 vtk 体渲染 + DensityColorLegend；isStaticFiguresOnly 时回退 PNG。

**答辩 30 秒**：中栏支持 cinematic 体渲染、渐进高清和单击聚焦；无 WebGL 时回退预渲染 PNG，答辩演示不翻车。

## VideoRightColumn.tsx

**路径**：[`src/dashboard/video-scenes/layout/VideoRightColumn.tsx`](../../../src/dashboard/video-scenes/layout/VideoRightColumn.tsx)

**职责**：右栏按场景显示光照图、形态面板、演化配图、直方图刷选+四格投影或早停验证 KPI。

**为什么选这段**：展示右栏刷选验证组合。

```123:190:src/dashboard/video-scenes/layout/VideoRightColumn.tsx
  return (
    <aside
      className={`vd-panel vd-panel-right${isValidateScene ? ' vd-panel-right--validate' : ''}`}
    >
      <header className="vd-panel-head">
        <h2>相空间刷选与空间验证</h2>
      </header>
      <div className="vd-panel-brush-hist">
        <DensityHistogram timeline={timeline} sizeOpts={histogramSizeOpts} />
      </div>
      {!isValidateScene && (
        <p className="vd-brush-hist-hint" aria-hidden>
          <span className="vd-brush-hist-arrow">↓</span>
          刷选密度区间 · 下方空间投影对应验证
        </p>
      )}
        {stats && (
        <VideoBrushPreviews
          stats={stats}
          densityData={densityData}
          loading={loading}
          dataMin={dataMin}
          dataMax={dataMax}
          activePreset={activePreset}
          onTop1={onTop1}
          onFilament={onFilament}
          onBottom1={onBottom1}
        />
      )}
      <div className="vd-brush-controls">
        <div className="vd-brush-presets">
          <button
            type="button"
            className={`vd-preset top${activePreset === 'top' ? ' on' : ''}`}
            onClick={onTop1}
          >
            Top 1%
          </button>
          <button
            type="button"
            className={`vd-preset fil${activePreset === 'filament' ? ' on' : ''}`}
            onClick={onFilament}
          >
            90–99%
          </button>
          <button
            type="button"
            className={`vd-preset bot${activePreset === 'bottom' ? ' on' : ''}`}
            onClick={onBottom1}
          >
            Bottom 1%
          </button>
          <button type="button" className="vd-preset clear" onClick={onClear}>
            清除
          </button>
        </div>
        {brushRange && stats && (
          <p className="vd-brush-readout">
            ρ∈[{brushRange.min.toFixed(2)}, {brushRange.max.toFixed(2)}]
            {volumeRatio != null && ` · 体积 ${volumeRatio}%`}
            {massPct != null && ` · 质量 ${massPct}%`}
          </p>
        )}
      </div>
      {sceneId === 'task4-validate' && (
        <VideoBrushValidationPanel brushValidation={videoStats.brushValidation} />
      )}
    </aside>
```

**输入 / 输出**：输入刷选回调、densityData、stats、brushValidation → 输出直方图+投影四格+预设按钮。

**答辩 30 秒**：任务四核心在右栏：直方图框选驱动体渲染高亮，下方四格 XY 投影用同一密度阈值验证 filament/void 结构。

## VideoFindingsRow.tsx

**路径**：[`src/dashboard/video-scenes/layout/VideoFindingsRow.tsx`](../../../src/dashboard/video-scenes/layout/VideoFindingsRow.tsx)

**职责**：底栏容器：按 showFindings 渲染 VideoFindingsStrip，findings 专场景加 focus 样式。

**为什么选这段**：展示底栏发现卡容器。

```11:20:src/dashboard/video-scenes/layout/VideoFindingsRow.tsx
  return (
    <div
      className={`vd-bottom${showFindings ? ' vd-bottom--findings-focus' : ''}${sceneId === 'findings' ? ' vd-bottom--findings-only' : ''}`}
    >
      {showFindings && (
        <VideoFindingsStrip timeline={timeline} focusMode={sceneId === 'findings'} />
      )}
      <div className="vd-letterbox" aria-hidden />
    </div>
  );
```

**输入 / 输出**：输入 showFindings、sceneId、timeline → 输出四张发现卡条带或空底栏。

**答辩 30 秒**：intro 和 findings 场景底部有四张科学发现卡；其他场景通过 sceneRegistry 把 --vd-findings-h 设为 0 腾出中栏。

## 录屏 scene 组件

## VideoSceneChrome.tsx

**路径**：[`src/dashboard/video-scenes/VideoSceneChrome.tsx`](../../../src/dashboard/video-scenes/VideoSceneChrome.tsx)

**职责**：录屏模式下顶栏下显示单行场景标题，便于 OBS 分段识别。

**为什么选这段**：展示录屏标题水印。

```6:14:src/dashboard/video-scenes/VideoSceneChrome.tsx
/** 录屏模式：header 下方单行场景标题，便于分段识别 */
export function VideoSceneChrome({ title, recordMode }: VideoSceneChromeProps) {
  if (!recordMode) return null;
  return (
    <div className="vd-scene-chrome" aria-hidden="true">
      <span className="vd-scene-chrome-label">{title}</span>
    </div>
  );
}
```

**输入 / 输出**：输入 title、recordMode → 录屏时输出标题条，预览时 null。

**答辩 30 秒**：录屏每段 URL 对应场景名水印，后期剪辑不用猜当前在讲任务几。

## VideoSceneNav.tsx

**路径**：[`src/dashboard/video-scenes/VideoSceneNav.tsx`](../../../src/dashboard/video-scenes/VideoSceneNav.tsx)

**职责**：预览模式显示 11 场景 tab；录屏模式显示「复制分段 URL」按钮。

**为什么选这段**：展示场景导航双模式。

```14:52:src/dashboard/video-scenes/VideoSceneNav.tsx
  if (recordMode) {
    const url =
      typeof window !== 'undefined'
        ? `${window.location.origin}${sceneUrl(currentScene, true)}`
        : sceneUrl(currentScene, true);
    const title = VIDEO_SCENES.find((s) => s.id === currentScene)?.title ?? currentScene;

    return (
      <div className="vd-scene-record-copy">
        <span className="vd-scene-record-label">录屏场景 · {title}</span>
        <button
          type="button"
          className="vd-scene-copy-btn"
          onClick={() => {
            void navigator.clipboard.writeText(url);
          }}
        >
          复制分段 URL
        </button>
      </div>
    );
  }

  return (
    <div className="vd-scene-strip" role="tablist" aria-label="预览场景">
      {VIDEO_SCENES.map((s) => (
        <button
          key={s.id}
          type="button"
          role="tab"
          aria-selected={currentScene === s.id}
          className={`vd-scene-tab${currentScene === s.id ? ' on' : ''}`}
          onClick={() => onSceneChange(s.id)}
        >
          {s.title}
        </button>
      ))}
    </div>
  );
```

**输入 / 输出**：输入 currentScene、onSceneChange → tab 条或复制 URL 按钮。

**答辩 30 秒**：11 场景各有 ?scene= URL，录屏按段打开即可；预览模式可 tab 快切彩排。

## VideoSceneRecordBrowse.tsx

**路径**：[`src/dashboard/video-scenes/VideoSceneRecordBrowse.tsx`](../../../src/dashboard/video-scenes/VideoSceneRecordBrowse.tsx)

**职责**：录屏浏览模式嵌入 main-stack 顶部的 1–11 场景编号快切条。

**为什么选这段**：展示录屏 browse 导航。

```12:31:src/dashboard/video-scenes/VideoSceneRecordBrowse.tsx
  return (
    <nav className="vd-scene-browse" aria-label="录屏 11 段场景">
      <div className="vd-scene-browse-track" role="tablist">
        {VIDEO_SCENES.map((s, index) => (
          <button
            key={s.id}
            type="button"
            role="tab"
            aria-selected={currentScene === s.id}
            className={`vd-scene-browse-btn${currentScene === s.id ? ' on' : ''}`}
            title={s.title}
            onClick={() => onSceneChange(s.id)}
          >
            <span className="vd-scene-browse-num">{index + 1}</span>
            <span className="vd-scene-browse-label">{sceneBrowseLabel(s)}</span>
          </button>
        ))}
      </div>
    </nav>
  );
```

**输入 / 输出**：输入 VIDEO_SCENES、currentScene → 编号 pill 导航。

**答辩 30 秒**：彩排时用顶栏 1–11 快切检查每段布局；正式录屏仍建议独立 URL 保证画面无导航条。

## VideoDashboardHeader.tsx

**路径**：[`src/dashboard/VideoDashboardHeader.tsx`](../../../src/dashboard/VideoDashboardHeader.tsx)

**职责**：录屏顶栏：品牌、演化阶段 pill、时间滑条、代表步按钮与当前 σ/p99 读数。

**为什么选这段**：展示时间轴控件。

```75:109:src/dashboard/VideoDashboardHeader.tsx
      <div className="vd-header-right">
        <div className="vd-time-readout">
          <span className="vd-time-label">t =</span>
          <strong className="vd-time-value">{sliderStep}</strong>
        </div>
        <input
          className="vd-time-slider"
          type="range"
          min={0}
          max={timestepCount - 1}
          value={sliderStep}
          onChange={(e) => onSliderChange(Number(e.target.value))}
          onPointerDown={onSliderDragStart}
          onMouseUp={onSliderCommit}
          onTouchEnd={onSliderCommit}
        />
        <div className="vd-step-buttons">
          {MARK_STEPS.map((t) => (
            <button
              key={t}
              type="button"
              className={`vd-step-btn${timestep === t ? ' on' : ''}`}
              onClick={() => onSelectTimestep(t)}
            >
              {t}
            </button>
          ))}
        </div>
        {stats && (
          <span className="vd-header-stats">
            sigma={stats.std.toFixed(4)} · p99={stats.p99.toFixed(4)}
            {!recordMode && ` · mu=${stats.mean.toFixed(2)}`}
          </span>
        )}
      </div>
```

**输入 / 输出**：输入 timestep、sliderStep、stats、回调 → 顶栏 UI。

**答辩 30 秒**：滑条拖动只更新预览步，松手 commitTimestep 才换体数据，避免拖动时反复 fetch 卡顿。

## VideoKpiStrip.tsx

**路径**：[`src/dashboard/VideoKpiStrip.tsx`](../../../src/dashboard/VideoKpiStrip.tsx)

**职责**：从 timeline 与当前步 stats 构建四张 KPI 卡；showSigma 切换 σ 与 σ² 旁白口径。

**为什么选这段**：展示 buildKpiItems 逻辑。

```8:54:src/dashboard/VideoKpiStrip.tsx
export function buildKpiItems(
  timeline: TimelineData,
  stats: DensityStats,
  mode: KpiDisplayMode = 'variance',
): KpiItem[] {
  const s0 = timeline.timesteps[0]!;
  const var0 = s0.std * s0.std;
  const varN = stats.std * stats.std;
  const varMult = var0 > 0 ? varN / var0 : 1;
  const tail0 = s0.tailMassAboveP99 * 100;
  const tailN = stats.tailMassAboveP99 * 100;
  const tailMult = tail0 > 1e-6 ? tailN / tail0 : 1;

  return [
    mode === 'sigma'
      ? {
          label: NARRATION_LABELS.sigma,
          value: stats.std.toFixed(4),
          badge: `+${(((stats.std - s0.std) / s0.std) * 100).toFixed(1)}% vs t=0`,
          tone: 'gold' as const,
        }
      : {
          label: '密度方差 σ²',
          value: varN.toFixed(2),
          badge: `↑${varMult.toFixed(1)}× vs 初值`,
          tone: 'gold' as const,
        },
    {
      label: NARRATION_LABELS.tailAbove,
      value: `${tailN.toFixed(2)}%`,
      badge: tailMult > 1 ? `↑${tailMult.toFixed(1)}×` : undefined,
      tone: 'orange' as const,
    },
    {
      label: NARRATION_LABELS.tailBelow,
      value: `${(stats.tailMassBelowP01 * 100).toFixed(1)}%`,
      tone: 'cyan' as const,
    },
    {
      label: NARRATION_LABELS.mean,
      value: stats.mean >= 100 || stats.mean < 0.01
        ? stats.mean.toExponential(2)
        : stats.mean.toFixed(2),
      tone: 'blue' as const,
    },
  ];
}
```

**输入 / 输出**：输入 timeline、stats、showSigma? → 四张 KPI 卡片。

**答辩 30 秒**：KPI 全从 timeline.json 读，badge 自动算相对 t=0 增幅；与 Word 表格、matplotlib 配图共用同一真源。

## VideoFindingsStrip.tsx

**路径**：[`src/dashboard/VideoFindingsStrip.tsx`](../../../src/dashboard/VideoFindingsStrip.tsx)

**职责**：底部四张科学发现卡（宇宙网、两极化、质量集中、统计—空间验证），支持 lightbox 放大。

**为什么选这段**：展示发现卡网格与弹层。

```277:310:src/dashboard/VideoFindingsStrip.tsx
export function VideoFindingsStrip({ timeline, focusMode = false }: VideoFindingsStripProps) {
  const m = computeStoryMetrics(timeline);
  const s = timeline.timesteps[99]!;
  const [expanded, setExpanded] = useState<FindingId | null>(null);
  const openCard = useCallback((id: FindingId) => setExpanded(id), []);
  const closeCard = useCallback(() => setExpanded(null), []);

  return (
    <section
      className={`vd-findings${focusMode ? ' vd-findings--focus' : ''}`}
      aria-label="科学发现摘要"
    >
      <div className="vd-findings-grid">
        <FindingEvoCard variant="strip" onExpand={() => openCard('01')} />
        <FindingMetricsCard variant="strip" m={m} onExpand={() => openCard('02')} />
        <FindingMassCard
          variant="strip"
          m={m}
          focusMode={focusMode}
          compactMass={!focusMode}
          onExpand={() => openCard('03')}
        />
        <FindingVerifyCard variant="strip" s={s} onExpand={() => openCard('04')} />
      </div>

      <OverlayLightbox
        open={expanded != null}
        label={expanded ? FINDING_LABELS[expanded] : ''}
        onClose={closeCard}
      >
        {expanded && renderLightboxCard(expanded, m, s)}
      </OverlayLightbox>
    </section>
  );
```

**输入 / 输出**：输入 timeline、focusMode? → 四卡网格 + OverlayLightbox。

**答辩 30 秒**：四卡把全文结论可视化：演化缩略图、σ 曲线、体积—质量对比、Top/Bottom 投影验证，一屏讲完故事线。

## VideoSpatialPanel.tsx

**路径**：[`src/dashboard/video-scenes/VideoSpatialPanel.tsx`](../../../src/dashboard/video-scenes/VideoSpatialPanel.tsx)

**职责**：task2-spatial 专用：Moran I、ξ(r) 等空间统计配图 + bootstrap 辅证数据表。

**为什么选这段**：展示空间统计场景布局。

```32:66:src/dashboard/video-scenes/VideoSpatialPanel.tsx
export function VideoSpatialPanel({ validation }: VideoSpatialPanelProps) {
  const bs = validation?.bootstrapSpatial;

  return (
    <div className="vd-spatial-scene">
      <div className="vd-spatial-figures">
        <div className="vd-spatial-metrics-grid">
          {METRIC_PANELS.map((panel) => (
            <figure key={panel.src} className="vd-spatial-metric">
              <div className="vd-spatial-metric-body">
                <ZoomableImage src={panel.src} alt={panel.alt} loading="eager" />
              </div>
              <figcaption className="vd-spatial-metric-caption">{panel.caption}</figcaption>
            </figure>
          ))}
        </div>
        <figure className="vd-spatial-wide-figure">
          <ZoomableImage
            src={figuresUrl('task2_two_point_xi.png')}
            alt="ξ(r) 剖面 + 子块 MC ±1σ"
            loading="eager"
          />
          <figcaption>ξ(r) 剖面 + 子块 MC ±1σ</figcaption>
        </figure>
      </div>

      <aside className="vd-scene-panel vd-spatial-stats">
        <header className="vd-scene-panel-head">
          <h3>空间统计辅证</h3>
          <p className="vd-scene-panel-sub">
            Moran I {bs ? `${bs.moransIGlobal.t0.toFixed(4)}→${bs.moransIGlobal.t99.toFixed(4)}` : '—'}
            ；ξ(r=1){' '}
            {bs ? `${bs.xiR1Global.t0.toFixed(3)}→${bs.xiR1Global.t99.toFixed(3)}` : '—'}
            ；增量未达 2σ 显著
          </p>
```

**输入 / 输出**：输入 validationExtended → 配图网格 + 右侧 bootstrap 读数。

**答辩 30 秒**：空间统计作辅证：展示 Moran I、ξ(r=1) 时序图和 2σ 显著性结论，主证仍是 σ 与 p99−p01 百步曲线。

## VideoVoidPanel.tsx

**路径**：[`src/dashboard/video-scenes/VideoVoidPanel.tsx`](../../../src/dashboard/video-scenes/VideoVoidPanel.tsx)

**职责**：展示 void 占比：用 t=0 固定的 p10/p01 阈值统计低密度体积分率跨步变化。

**为什么选这段**：展示 void 双阈值卡片。

```17:60:src/dashboard/video-scenes/VideoVoidPanel.tsx
  const { t0, t99 } = validation.voidFractions;

  return (
    <div className="vd-scene-panel vd-void-panel">
      <header className="vd-scene-panel-head">
        <h3>void 占比 · 统计阈值口径</h3>
        <p className="vd-scene-panel-sub">
          子体积无全宇宙平均密度标定，改用 t=0 分位阈值 · p10：10.00%→
          {t99.belowT0P10.toFixed(2)}% · p01：1.00%→{t99.belowT0P01.toFixed(2)}%
        </p>
      </header>

      <div className="vd-void-grid">
        <article className="vd-void-card">
          <h4>阈值：t=0 的 p10</h4>
          <div className="vd-void-values">
            <div>
              <span className="vd-void-label">t=0</span>
              <strong>{t0.belowT0P10.toFixed(2)}%</strong>
            </div>
            <span className="vd-void-arrow">→</span>
            <div>
              <span className="vd-void-label">t=99</span>
              <strong>{t99.belowT0P10.toFixed(2)}%</strong>
            </div>
          </div>
          <p className="vd-void-note">初始占比 ≈ 10.00%</p>
        </article>

        <article className="vd-void-card tone-cyan">
          <h4>阈值：t=0 的 p01</h4>
          <div className="vd-void-values">
            <div>
              <span className="vd-void-label">t=0</span>
              <strong>{t0.belowT0P01.toFixed(2)}%</strong>
            </div>
            <span className="vd-void-arrow">→</span>
            <div>
              <span className="vd-void-label">t=99</span>
              <strong>{t99.belowT0P01.toFixed(2)}%</strong>
            </div>
          </div>
          <p className="vd-void-note">低密度尾扩张更明显</p>
        </article>
```

**输入 / 输出**：输入 validation.voidFractions → 两张阈值对比卡。

**答辩 30 秒**：void 定义用 t=0 固定分位阈值保证跨步可比：p10 约 10%→24.69%，说明低密度尾扩张，与 Word §3 一致。

## VideoVoidScene.tsx

**路径**：[`src/dashboard/video-scenes/VideoVoidScene.tsx`](../../../src/dashboard/video-scenes/VideoVoidScene.tsx)

**职责**：task2-void 场景组合：VideoVoidPanel 统计卡 + void 演化配图。

**为什么选这段**：展示 void 场景两栏布局。

```10:25:src/dashboard/video-scenes/VideoVoidScene.tsx
export function VideoVoidScene({ validation }: VideoVoidSceneProps) {
  return (
    <div className="vd-body vd-body--void">
      <div className="vd-void-col vd-void-col--stats">
        <VideoVoidPanel validation={validation} />
      </div>
      <figure className="vd-void-figure vd-void-col--chart">
        <ZoomableImage
          src={figuresUrl('task3_void_evolution.png')}
          alt="void 占比随时间演化"
          loading="eager"
        />
        <figcaption>低密度 void 占比演化（p10 / p01 阈值）</figcaption>
      </figure>
    </div>
  );
```

**输入 / 输出**：输入 validation → 左统计右配图双栏。

**答辩 30 秒**：void 场景左数字右曲线，口述「固定 t=0 阈值看低密度体积分数怎么涨」时指这两块即可。

## VideoMorphPanel.tsx

**路径**：[`src/dashboard/video-scenes/VideoMorphPanel.tsx`](../../../src/dashboard/video-scenes/VideoMorphPanel.tsx)

**职责**：task1-morph 右栏：五代表步 EvolutionThumbnails + 当前步 σ/p99 读数。

**为什么选这段**：展示形态演化面板。

```20:61:src/dashboard/video-scenes/VideoMorphPanel.tsx
export function VideoMorphPanel({
  timeline,
  timestep,
  stats,
  onSelectTimestep,
}: VideoMorphPanelProps) {
  const stage =
    MORPH_STAGES.find((s) => s.t === timestep)?.label ??
    `t=${timestep}`;

  return (
    <div className="vd-scene-panel vd-morph-panel">
      <header className="vd-scene-panel-head">
        <h3>形态演化 · 五代表步</h3>
        <p className="vd-scene-panel-sub">点击缩略图标签切换 t；点击图片放大</p>
      </header>

      <EvolutionThumbnails
        timeline={timeline}
        active={timestep}
        onSelect={onSelectTimestep}
      />

      <p className="vd-morph-stage" aria-live="polite">
        {stage}
      </p>

      <dl className="vd-spec-dl vd-morph-readout-dl">
        <div>
          <dt>{NARRATION_LABELS.sigma}</dt>
          <dd>{stats.std.toFixed(4)}</dd>
        </div>
        <div>
          <dt>p99</dt>
          <dd>{stats.p99.toFixed(4)}</dd>
        </div>
        <div>
          <dt>{NARRATION_LABELS.mean}</dt>
          <dd>{stats.mean.toFixed(4)}</dd>
        </div>
      </dl>
    </div>
```

**输入 / 输出**：输入 timeline、timestep、stats、onSelectTimestep → 缩略图条+阶段文案。

**答辩 30 秒**：形态演化场景用五帧 XY 投影缩略图讲故事，点击切 t 同步中栏体渲染，右栏显示当前步关键分位数。

## VideoEvolutionPanel.tsx

**路径**：[`src/dashboard/video-scenes/VideoEvolutionPanel.tsx`](../../../src/dashboard/video-scenes/VideoEvolutionPanel.tsx)

**职责**：task2-evolution 左栏：σ、p99−p01、p99.9 从 t=0 到 t=99 的量化对比卡。

**为什么选这段**：展示演化量化三指标。

```8:55:src/dashboard/video-scenes/VideoEvolutionPanel.tsx
export function VideoEvolutionPanel({ timeline }: VideoEvolutionPanelProps) {
  const m = computeStoryMetrics(timeline);
  const p999Mult = m.s0.p999 > 0 ? m.s99.p999 / m.s0.p999 : 1;

  const rows = [
    {
      label: '线性密度 σ',
      from: m.s0.std.toFixed(4),
      to: m.s99.std.toFixed(4),
      delta: `+${m.sigmaPct.toFixed(1)}%`,
      tone: 'cyan' as const,
    },
    {
      label: 'p99−p01 分位跨度',
      from: m.span0.toFixed(3),
      to: m.span99.toFixed(3),
      delta: `+${m.spanPct.toFixed(1)}%`,
      tone: 'violet' as const,
    },
    {
      label: 'p99.9 相对 t=0',
      from: m.s0.p999.toFixed(3),
      to: m.s99.p999.toFixed(3),
      delta: `×${p999Mult.toFixed(3)}`,
      tone: 'gold' as const,
    },
  ];

  return (
    <div className="vd-scene-panel vd-evolution-panel">
      <header className="vd-scene-panel-head">
        <h3>演化量化 · 团块化与两极化</h3>
      </header>
      <div className="vd-evolution-grid">
        {rows.map((r) => (
          <div key={r.label} className={`vd-evolution-card tone-${r.tone}`}>
            <span className="vd-evolution-label">{r.label}</span>
            <div className="vd-evolution-values">
              <span>{r.from}</span>
              <span className="vd-evolution-arrow">→</span>
              <strong>{r.to}</strong>
            </div>
            <span className="vd-evolution-delta">{r.delta}</span>
          </div>
        ))}
      </div>
    </div>
  );
```

**输入 / 输出**：输入 timeline → 三张演化对比卡（含 delta badge）。

**答辩 30 秒**：三张卡直接回答任务二：团块化看 σ 和分位跨度涨多少，右尾看 p99.9 相对初值几倍。

## VideoBrushGuidePanel.tsx

**路径**：`src/dashboard/video-scenes/VideoBrushGuidePanel.tsx`（**当前仓库不存在**）

**职责**：原计划为 task4-brush 场景的刷选操作引导面板；功能已并入 `VideoRightColumn` 的直方图提示（`vd-brush-hist-hint`）与预设按钮区，无独立文件。

**为什么选这段**：清单列出但源码未实现，答辩时说明合并位置即可。

（无代码摘录）

**输入 / 输出**：— 

**答辩 30 秒**：刷选引导没有单独组件，右栏直方图下方的箭头和 Top 1%/90–99%/Bottom 1% 预设按钮就是操作引导；交互逻辑在 useDashboardInteraction。

## VideoBrushValidationPanel.tsx

**路径**：[`src/dashboard/video-scenes/VideoBrushValidationPanel.tsx`](../../../src/dashboard/video-scenes/VideoBrushValidationPanel.tsx)

**职责**：task4-validate 右栏：展示 brush_validation.json 的离线召回率、精确率、Worker 早停耗时。

**为什么选这段**：展示早停验证 KPI 网格。

```25:58:src/dashboard/video-scenes/VideoBrushValidationPanel.tsx
  return (
    <div className="vd-scene-panel vd-brush-validate">
      <header className="vd-scene-panel-head vd-brush-validate-head">
        <h3>离线验证 · 早停采样 KPI</h3>
      </header>

      <div className="vd-validate-grid">
        <div className="vd-validate-card">
          <span className="vd-validate-label">离线召回</span>
          <strong>{(fpFnDefault.recall * 100).toFixed(1)}%</strong>
        </div>
        <div className="vd-validate-card tone-orange">
          <span className="vd-validate-label">精确率</span>
          <strong>{(fpFnDefault.precision * 100).toFixed(1)}%</strong>
        </div>
        <div className="vd-validate-card tone-cyan">
          <span className="vd-validate-label">Top1% Worker</span>
          <strong>{benchmark.top1_earlyExit.elapsedMs.toFixed(0)} ms</strong>
          <span className="vd-validate-sub">
            全量 {benchmark.top1_fullCount.elapsedMs.toFixed(0)} ms
          </span>
        </div>
        <div className="vd-validate-card">
          <span className="vd-validate-label">Top1% 早停体素覆盖</span>
          <strong>{top1Sample.toFixed(1)}%</strong>
        </div>
        {p25p75 && (
          <div className="vd-validate-card tone-violet">
            <span className="vd-validate-label">p25–p75 早停覆盖</span>
            <strong>{p25p75.recallVsTruePct.toFixed(2)}%</strong>
          </div>
        )}
      </div>
    </div>
```

**输入 / 输出**：输入 brushValidation → 验证 KPI 卡片网格。

**答辩 30 秒**：自定义刷选 KPI 用 Worker 早停采样：离线召回 100%、Top1% 早停几十毫秒，3D 高亮仍对全场 128³ 生效。

## VideoHistMethodStrip.tsx

**路径**：[`src/dashboard/video-scenes/VideoHistMethodStrip.tsx`](../../../src/dashboard/video-scenes/VideoHistMethodStrip.tsx)

**职责**：task3-hist 左栏：log 等距直方图分箱敏感性说明与 skew 演化读数。

**为什么选这段**：展示分箱方法条。

```21:56:src/dashboard/video-scenes/VideoHistMethodStrip.tsx
  return (
    <div className="vd-scene-panel vd-hist-method">
      <header className="vd-scene-panel-head">
        <h3>log 等距直方图 · 分箱敏感性</h3>
      </header>
      <dl className="vd-spec-dl">
        <div>
          <dt>默认分箱</dt>
          <dd>{timeline.binCount} bins</dd>
        </div>
        <div>
          <dt>边界</dt>
          <dd>
            {timeline.globalMin.toFixed(3)} – {timeline.globalMax.toFixed(3)}
          </dd>
        </div>
        <div>
          <dt>CDF L∞ (64/256 vs 128)</dt>
          <dd>≤ {maxCdf > 0 ? maxCdf.toFixed(4) : '0.0012'}</dd>
        </div>
        <div>
          <dt>{NARRATION_LABELS.tailAbove}</dt>
          <dd>{(s99.tailMassAboveP99 * 100).toFixed(2)}%</dd>
        </div>
        <div>
          <dt>偏度 skew</dt>
          <dd>
            {s0.skewness.toFixed(4)} → {s99.skewness.toFixed(4)}
          </dd>
        </div>
      </dl>
      <p className="vd-hist-method-note">
        线性分箱易淹没低密度 void 细节；log 等距直方图更好展示 IGM 与 filament 差异。
        128 bins 为默认。
      </p>
    </div>
```

**输入 / 输出**：输入 timeline、validation → 方法说明 dl 列表。

**答辩 30 秒**：说明为何用 log 等距 128 bins：线性分箱淹没 void 细节，分箱敏感性 CDF L∞≤0.0012 证明默认分箱稳定。

## VideoRenderSpecPanel.tsx

**路径**：[`src/dashboard/video-scenes/VideoRenderSpecPanel.tsx`](../../../src/dashboard/video-scenes/VideoRenderSpecPanel.tsx)

**职责**：task1-tf 左栏：cosmic 传递函数颜色条、7 点不透明度阶梯、Phong 系数与光源参数。

**为什么选这段**：展示 renderSpec 可视化。

```57:107:src/dashboard/video-scenes/VideoRenderSpecPanel.tsx
  return (
    <div className="vd-scene-panel vd-render-spec">
      <header className="vd-scene-panel-head">
        <h3>cosmic 传递函数 · Phong 光照</h3>
        <p className="vd-scene-panel-sub">
          log₁₀ 域 ρ：p01 {p01.toFixed(3)} → p99 {p99.toFixed(3)}
        </p>
      </header>

      <section className="vd-spec-block">
        <h4>颜色映射</h4>
        <ColorTfBar stops={renderSpec.colorStopsNormalized} />
      </section>

      <section className="vd-spec-block">
        <h4>不透明度 α（7 控制点）</h4>
        <div className="vd-alpha-ladder">
          {stops.map(([t, alpha], i) => (
            <div key={i} className="vd-alpha-step">
              <span className="vd-alpha-t">{(t * 100).toFixed(0)}%</span>
              <div
                className="vd-alpha-bar"
                style={{ opacity: Math.max(0.15, alpha) }}
              />
              <span className="vd-alpha-val">α={alpha.toFixed(2)}</span>
            </div>
          ))}
        </div>
      </section>

      <section className="vd-spec-block">
        <h4>Phong 光照</h4>
        <dl className="vd-spec-dl">
          <div>
            <dt>Ka</dt>
            <dd>{pres.ambient.toFixed(2)}</dd>
          </div>
          <div>
            <dt>Kd</dt>
            <dd>{pres.diffuse.toFixed(2)}</dd>
          </div>
          <div>
            <dt>Ks</dt>
            <dd>{pres.specular.toFixed(2)}</dd>
          </div>
          <div>
            <dt>sampleDistance</dt>
            <dd>{pres.sampleDistance.toFixed(2)}</dd>
          </div>
        </dl>
      </section>
```

**输入 / 输出**：输入 renderSpec、timeline → 参数表 + ColorTfBar。

**答辩 30 秒**：传递函数参数来自 render_spec.json，左栏把颜色映射和不透明度阶梯画出来，右栏配光照示意图。

## VideoFigureStrip.tsx

**路径**：[`src/dashboard/video-scenes/VideoFigureStrip.tsx`](../../../src/dashboard/video-scenes/VideoFigureStrip.tsx)

**职责**：通用配图条：按 row/stack 布局渲染多张可放大静态图。

**为什么选这段**：展示配图条组件。

```9:26:src/dashboard/video-scenes/VideoFigureStrip.tsx
export function VideoFigureStrip({
  figures,
  layout = 'row',
  className,
}: VideoFigureStripProps) {
  const stripClass = ['vd-figure-strip', `vd-figure-strip--${layout}`, className]
    .filter(Boolean)
    .join(' ');
  return (
    <div className={stripClass}>
      {figures.map((fig) => (
        <figure key={fig.src} className="vd-figure-strip-item">
          <ZoomableImage src={fig.src} alt={fig.alt ?? fig.caption} loading="lazy" />
          {fig.caption && <figcaption>{fig.caption}</figcaption>}
        </figure>
      ))}
    </div>
  );
```

**输入 / 输出**：输入 figures[]、layout → 配图 figure 列表。

**答辩 30 秒**：task3-hist 右栏时序指标图用此组件堆叠展示，统一 ZoomableImage 交互。

## VideoCaseCards.tsx

**路径**：[`src/dashboard/video-scenes/VideoCaseCards.tsx`](../../../src/dashboard/video-scenes/VideoCaseCards.tsx)

**职责**：task2-cases 左栏：案例 A/B/C（百步曲线、Top1% 质量集中、P88 亮脊反查）可点击放大。

**为什么选这段**：展示三案例卡。

```135:163:src/dashboard/video-scenes/VideoCaseCards.tsx
export function VideoCaseCards({
  brushValidation,
  storyMetrics: m,
}: VideoCaseCardsProps) {
  const p88 = brushValidation?.p88Sweep.find((s) => s.projPercentile === 88);
  const band = p88?.densityBand ?? brushValidation?.fpFnDefault.filamentBand ?? null;
  const [expanded, setExpanded] = useState<CaseId | null>(null);
  const openCard = useCallback((id: CaseId) => setExpanded(id), []);
  const closeCard = useCallback(() => setExpanded(null), []);

  return (
    <div className="vd-scene-panel vd-case-cards">
      <header className="vd-scene-panel-head">
        <h3>可视化价值 · 三案例</h3>
      </header>

      <CaseCardA variant="strip" onExpand={() => openCard('a')} />
      <CaseCardB variant="strip" m={m} onExpand={() => openCard('b')} />
      <CaseCardC variant="strip" band={band} onExpand={() => openCard('c')} />

      <OverlayLightbox
        open={expanded != null}
        label={expanded ? CASE_LABELS[expanded] : ''}
        onClose={closeCard}
      >
        {expanded && renderLightboxCard(expanded, m, band)}
      </OverlayLightbox>
    </div>
  );
```

**输入 / 输出**：输入 brushValidation、storyMetrics → 三案例卡 + lightbox。

**答辩 30 秒**：三案例说明可视化价值：没有百步曲线会挑帧叙事；Top1% 体积小质量大；P88 亮脊反查密度与 Top1% 一致。

## VideoEvolutionFigurePanel.tsx

**路径**：[`src/dashboard/video-scenes/VideoEvolutionFigurePanel.tsx`](../../../src/dashboard/video-scenes/VideoEvolutionFigurePanel.tsx)

**职责**：task2-evolution 右栏：四张演化指标配图（σ、分位跨度、尾段体积、skew）。

**为什么选这段**：展示演化配图网格。

```27:41:src/dashboard/video-scenes/VideoEvolutionFigurePanel.tsx
export function VideoEvolutionFigurePanel() {
  return (
    <div className="vd-evolution-figures">
      <div className="vd-evolution-metrics-grid">
        {EVOLUTION_PANELS.map((panel) => (
          <figure key={panel.src} className="vd-evolution-metric">
            <div className="vd-evolution-metric-body">
              <ZoomableImage src={panel.src} alt={panel.alt} loading="eager" />
            </div>
            <figcaption className="vd-evolution-metric-caption">{panel.caption}</figcaption>
          </figure>
        ))}
      </div>
    </div>
  );
```

**输入 / 输出**：无 props；读取 figuresUrl 静态 PNG → 2×2 配图网格。

**答辩 30 秒**：右栏四图与左栏数字互证，均来自 precompute 与 generate_figures，不是前端手画曲线。

## VideoValidateFigureColumn.tsx

**路径**：[`src/dashboard/video-scenes/VideoValidateFigureColumn.tsx`](../../../src/dashboard/video-scenes/VideoValidateFigureColumn.tsx)

**职责**：task4-validate 中栏：阈值对比、自定义 KPI 误差、Top1% 早停召回三张验证图。

**为什么选这段**：展示验证场景中栏配图。

```22:39:src/dashboard/video-scenes/VideoValidateFigureColumn.tsx
export function VideoValidateFigureColumn() {
  return (
    <main className="vd-panel vd-panel-center vd-panel-center--validate-figures">
      <header className="vd-panel-head">
        <h2>刷选验证摘要</h2>
      </header>
      <div className="vd-validate-figure-column">
        {VALIDATE_PANELS.map((panel) => (
          <figure key={panel.src} className="vd-validate-figure-cell">
            <div className="vd-validate-figure-body">
              <ZoomableImage src={panel.src} alt={panel.alt} loading="eager" />
            </div>
            <figcaption className="vd-validate-figure-caption">{panel.caption}</figcaption>
          </figure>
        ))}
      </div>
    </main>
  );
```

**输入 / 输出**：无 props → 三张验证 figure 纵列。

**答辩 30 秒**：验证场景中栏放离线 benchmark 配图，右栏放数值 KPI，口述「统计刷选可复现、早停可加速」。

## 辅助 viz 组件

## StarfieldBackground.tsx

**路径**：[`src/components/StarfieldBackground.tsx`](../../../src/components/StarfieldBackground.tsx)

**职责**：兼容层：将旧 StarfieldBackground 调用转发到 CosmicBackdrop（poster 变体）。

**为什么选这段**：展示 deprecated 兼容包装。

```10:21:src/components/StarfieldBackground.tsx
export function StarfieldBackground({
  className = '',
}: StarfieldBackgroundProps) {
  const fixed = className.includes('starfield-bg--fixed');
  const props: CosmicBackdropProps = {
    variant: 'poster',
    intensity: 'full',
    fixed,
    className: className.replace('starfield-bg--fixed', '').replace('starfield-bg', '').trim(),
  };
  return <CosmicBackdrop {...props} />;
}
```

**输入 / 输出**：输入 className → 输出 CosmicBackdrop poster 背景。

**答辩 30 秒**：长卷页背景已统一为 CosmicBackdrop；StarfieldBackground 保留以免旧 import 报错。

## ImageLightbox.tsx

**路径**：[`src/components/ImageLightbox.tsx`](../../../src/components/ImageLightbox.tsx)

**职责**：ZoomableImage 点击放大；OverlayLightbox 全屏弹层；LightboxZoomView 支持滚轮缩放与拖拽平移。

**为什么选这段**：展示 ZoomableImage 点击逻辑。

```237:276:src/components/ImageLightbox.tsx
export function ZoomableImage({
  className,
  alt = '',
  src,
  onClick,
  stopPropagation = true,
  ...rest
}: ZoomableImageProps) {
  const [open, setOpen] = useState(false);
  const [lightboxSrc, setLightboxSrc] = useState<string | undefined>(src);
  const close = useCallback(() => setOpen(false), []);

  const handleClick = useCallback(
    (e: MouseEvent<HTMLImageElement>) => {
      if (stopPropagation) {
        e.stopPropagation();
        e.nativeEvent.stopImmediatePropagation();
      }
      e.preventDefault();
      onClick?.(e);
      setLightboxSrc(e.currentTarget.currentSrc || e.currentTarget.src);
      setOpen(true);
    },
    [onClick, stopPropagation],
  );

  return (
    <>
      <img
        {...rest}
        src={src}
        alt={alt}
        className={['vd-zoomable', className].filter(Boolean).join(' ')}
        onClick={handleClick}
      />
      <OverlayLightbox open={open} label={alt || '图片预览'} onClose={close}>
        {lightboxSrc && <LightboxZoomView src={lightboxSrc} alt={alt} />}
      </OverlayLightbox>
    </>
  );
```

**输入 / 输出**：输入 img src/alt → 缩略图 + 可选全屏 zoom 视图。

**答辩 30 秒**：录屏大量静态配图支持点击放大到原图精度，评委追问细节时可现场 zoom 到像素级。

## HorizontalColorLegend.tsx

**路径**：[`src/dashboard/HorizontalColorLegend.tsx`](../../../src/dashboard/HorizontalColorLegend.tsx)

**职责**：横向密度 log₁₀ ρ 色标，用于 app 横版布局。

**为什么选这段**：展示横向色标条。

```8:29:src/dashboard/HorizontalColorLegend.tsx
export function HorizontalColorLegend({ min, max }: HorizontalColorLegendProps) {
  const logMin = Math.log10(Math.max(min, 1e-6));
  const logMax = Math.log10(Math.max(max, 1e-6));
  const mid = (logMax + logMin) / 2;

  return (
    <div className="horizontal-legend" aria-label="密度色标">
      <div className="horizontal-legend-head">
        <span className="horizontal-legend-title">密度 log₁₀ ρ</span>
        <span className="horizontal-legend-note">Nyx 全局 p01–p99</span>
      </div>
      <div
        className="horizontal-legend-bar"
        style={{ background: cosmicLegendGradient() }}
      />
      <div className="horizontal-legend-labels">
        <span>低密度 {logMin.toFixed(0)}</span>
        <span>中密度 {mid.toFixed(0)}</span>
        <span>高密度 {logMax.toFixed(0)}</span>
      </div>
    </div>
  );
```

**输入 / 输出**：输入 min、max（全局 p01–p99）→ 横向渐变 legend。

**答辩 30 秒**：色标与 transferFunction cosmic 渐变一致，标注 log 域三档：低/中/高密度。

## VerticalColorLegend.tsx

**路径**：[`src/dashboard/VerticalColorLegend.tsx`](../../../src/dashboard/VerticalColorLegend.tsx)

**职责**：纵向密度色标，用于 Cosmic 长卷 poster 左侧。

**为什么选这段**：展示纵向色标。

```8:33:src/dashboard/VerticalColorLegend.tsx
export function VerticalColorLegend({ min, max }: VerticalColorLegendProps) {
  const logMin = Math.log10(Math.max(min, 1e-6));
  const logMax = Math.log10(Math.max(max, 1e-6));
  const ticks = [logMax, (logMax + logMin) / 2, logMin];

  return (
    <div className="vertical-legend" aria-label="密度色标">
      <span className="vertical-legend-caption">密度</span>
      <span className="vertical-legend-unit">log₁₀ ρ</span>
      <div className="vertical-legend-wrap">
        <div
          className="vertical-legend-bar"
          style={{
            background: cosmicLegendGradient().replace('90deg', '0deg'),
          }}
        />
        <div className="vertical-legend-ticks">
          {ticks.map((t) => (
            <span key={t}>{t.toFixed(0)}</span>
          ))}
        </div>
      </div>
      <span className="vertical-legend-note">Nyx 全局 p01–p99</span>
    </div>
  );
}
```

**输入 / 输出**：输入 min、max → 纵向渐变 + 三档 tick。

**答辩 30 秒**：长卷 hero 区左侧竖色标，与体渲染/投影共用全局 p01–p99 域。

## VideoBrushPreviews.tsx

**路径**：[`src/dashboard/VideoBrushPreviews.tsx`](../../../src/dashboard/VideoBrushPreviews.tsx)

**职责**：录屏右栏四格密度带预览：共享 useSharedProjection，每格不同 brushRange 调 BandPreviewCanvas。

**为什么选这段**：展示四格预览与静态回退。

```55:124:src/dashboard/VideoBrushPreviews.tsx
export function VideoBrushPreviews({
  stats,
  densityData,
  loading,
  dataMin,
  dataMax,
  activePreset,
  onTop1,
  onFilament,
  onBottom1,
}: VideoBrushPreviewsProps) {
  const projection = useSharedProjection(densityData, 'xy');

  const handlers: Record<string, () => void> = {
    top: onTop1,
    filament: onFilament,
    bottom: onBottom1,
    mid: () => {},
  };

  const canRender = densityData && projection && !loading;
  const staticOnly = isStaticFiguresOnly();

  return (
    <div className="vd-band-previews">
      {BANDS.map((b) => {
        const range = bandRange(stats, b.id);
        const onClick = b.preset ? handlers[b.preset] : undefined;
        const staticSrc = staticOnly ? STATIC_BAND_FIGURES[b.id as BrushBandId] : undefined;
        return (
          <button
            key={b.id}
            type="button"
            className={`vd-band-cell band-${b.id}${activePreset === b.preset ? ' on' : ''}`}
            onClick={onClick}
            disabled={!onClick}
            title={b.label}
          >
            <span className="vd-band-label">{b.label}</span>
            <div className="vd-band-media">
              {canRender ? (
                <BandPreviewCanvas
                  projection={projection}
                  brushRange={range}
                  domainMin={dataMin}
                  domainMax={dataMax}
                  className="vd-band-proj"
                />
              ) : staticSrc ? (
                <img
                  className="vd-band-proj vd-band-static"
                  src={figuresUrl(staticSrc)}
                  alt={b.label}
                  loading="lazy"
                  onError={(e) => {
                    const fb = STATIC_BAND_FALLBACKS[b.id as BrushBandId];
                    if (fb && !e.currentTarget.dataset.fallback) {
                      e.currentTarget.dataset.fallback = '1';
                      e.currentTarget.src = figuresUrl(fb);
                    }
                  }}
                />
              ) : (
                <span className="vd-band-placeholder">…</span>
              )}
            </div>
          </button>
        );
      })}
    </div>
```

**输入 / 输出**：输入 stats、densityData、activePreset、刷选回调 → 四格可点击预览按钮。

**答辩 30 秒**：四格对应 Bottom/mid/filament/Top 密度带，点击同步预设刷选；无体数据时回退预渲染 PNG。

## Cosmic 长卷 viz 组件

## StoryKpiStrip.tsx

**路径**：[`src/dashboard/StoryKpiStrip.tsx`](../../../src/dashboard/StoryKpiStrip.tsx)

**职责**：app 长卷当前步四条 KPI：σ、≥p99 体积、≤p01 体积、均值 μ。

**为什么选这段**：展示 story KPI 条。

```7:25:src/dashboard/StoryKpiStrip.tsx
export function StoryKpiStrip({ stats }: StoryKpiStripProps) {
  const items = [
    { label: '标准差 σ', value: stats.std.toFixed(4), tone: 'cyan' },
    { label: '≥p99 体积', value: `${(stats.tailMassAboveP99 * 100).toFixed(2)}%`, tone: 'gold' },
    { label: '≤p01 体积', value: `${(stats.tailMassBelowP01 * 100).toFixed(2)}%`, tone: 'blue' },
    { label: '均值 μ', value: stats.mean.toFixed(3), tone: 'purple' },
  ];

  return (
    <div className="story-kpi-strip" aria-label={`t=${stats.timestep} 关键指标`}>
      {items.map((it) => (
        <div key={it.label} className={`story-kpi story-kpi-${it.tone}`}>
          <span className="story-kpi-label">{it.label}</span>
          <span className="story-kpi-value">{it.value}</span>
        </div>
      ))}
    </div>
  );
}
```

**输入 / 输出**：输入 stats（单步）→ 四格 KPI 条。

**答辩 30 秒**：长卷滚动到统计区块时，用当前步 stats 即时显示四个核心数，与录屏 KPI 口径一致。

## TimestepKpiCards.tsx

**路径**：[`src/dashboard/TimestepKpiCards.tsx`](../../../src/dashboard/TimestepKpiCards.tsx)

**职责**：探索面板六格 KPI 卡：μ、σ、p99−p01、尾段体积、max。

**为什么选这段**：展示 KPI 网格。

```7:27:src/dashboard/TimestepKpiCards.tsx
export function TimestepKpiCards({ stats }: TimestepKpiCardsProps) {
  const span = stats.p99 - stats.p01;
  const items = [
    { label: '均值 μ', value: stats.mean.toFixed(4) },
    { label: '标准差 σ', value: stats.std.toFixed(4) },
    { label: 'p99−p01', value: span.toFixed(3) },
    { label: '≥p99 体积', value: `${(stats.tailMassAboveP99 * 100).toFixed(2)}%` },
    { label: '≤p01 体积', value: `${(stats.tailMassBelowP01 * 100).toFixed(2)}%` },
    { label: 'max', value: stats.max.toFixed(3) },
  ];

  return (
    <div className="kpi-grid">
      {items.map(({ label, value }) => (
        <div key={label} className="kpi-card">
          <span className="kpi-label">{label}</span>
          <span className="kpi-value">{value}</span>
        </div>
      ))}
    </div>
  );
```

**输入 / 输出**：输入 stats → 六张 kpi-card。

**答辩 30 秒**：交互页右栏用更细的六指标网格，方便评委对照直方图分位数。

## HeroMetaBar.tsx

**路径**：[`src/dashboard/HeroMetaBar.tsx`](../../../src/dashboard/HeroMetaBar.tsx)

**职责**：长卷 hero 区元信息：128³、100 步、物理量 chips + t=99 σ 与 span 摘要。

**为什么选这段**：展示 hero 元数据条。

```6:27:src/dashboard/HeroMetaBar.tsx
export function HeroMetaBar({ sigma, spanPct }: HeroMetaBarProps) {
  const items = [
    { label: '128³ 网格', value: 'Nyx 模拟' },
    { label: '100 时间步', value: 't=0…99' },
    { label: '物理量', value: '气体密度 ρ' },
  ];

  return (
    <div className="pl-hero-meta">
      <div className="pl-hero-meta-chips">
        {items.map((it) => (
          <span key={it.label} className="pl-hero-chip">
            <strong>{it.label}</strong>
            <span>{it.value}</span>
          </span>
        ))}
      </div>
      <p className="pl-hero-meta-stats">
        t=99 · σ={sigma} · p99−p01 +{spanPct}%
      </p>
    </div>
  );
```

**输入 / 输出**：输入 sigma、spanPct 字符串 → hero chips + 统计一行。

**答辩 30 秒**：开篇 hero 下方一行交代数据规模与终态关键增幅，不用切页就能建立语境。

## EvolutionThumbnails.tsx

**路径**：[`src/dashboard/EvolutionThumbnails.tsx`](../../../src/dashboard/EvolutionThumbnails.tsx)

**职责**：五代表步 XY 投影缩略图条，点击切步、点击图片放大。

**为什么选这段**：展示缩略图切换逻辑。

```15:49:src/dashboard/EvolutionThumbnails.tsx
export function EvolutionThumbnails({ timeline, active, onSelect }: EvolutionThumbnailsProps) {
  return (
    <div className="evo-thumbs" role="list" aria-label="代表时间步">
      {MARK_STEPS.map((t) => {
        const stats = timeline.timesteps[t];
        const sigma = stats?.std.toFixed(3) ?? '—';
        return (
          <button
            key={t}
            type="button"
            role="listitem"
            className={`evo-thumb${active === t ? ' active' : ''}`}
            onClick={(e) => {
              // ZoomableImage 嵌在 button 内时，浏览器仍会激活 button；点击图片只放大，不切步
              if ((e.target as Element).closest('.vd-zoomable')) return;
              onSelect(t);
            }}
          >
            <div className="evo-thumb-media">
              <ZoomableImage
                src={evolutionThumbnailSrc(t)}
                alt={`t=${t} XY 最大密度投影演化`}
                loading="lazy"
                onError={(e) => handleEvolutionThumbnailError(e.currentTarget, t)}
              />
            </div>
            <span className="evo-thumb-label">
              t={t}
              <span className="evo-thumb-sigma">σ={sigma}</span>
            </span>
          </button>
        );
      })}
    </div>
  );
```

**输入 / 输出**：输入 timeline、active、onSelect → 五按钮缩略图条。

**答辩 30 秒**：五帧条带是任务一/二的视觉锚点；点击标签切 t 同步体渲染，点击图只放大不跳步。

## DiscoveryCards.tsx

**路径**：[`src/dashboard/DiscoveryCards.tsx`](../../../src/dashboard/DiscoveryCards.tsx)

**职责**：四张科学发现 tile + 可选质量对比 donut（poster 变体）。

**为什么选这段**：展示发现卡与质量面板。

```51:80:src/dashboard/DiscoveryCards.tsx
export function DiscoveryCards({ timeline, variant = 'default' }: DiscoveryCardsProps) {
  const m = computeStoryMetrics(timeline);
  const poster = variant === 'poster';
  const representative = variant === 'representative';

  return (
    <div
      className={`discovery-grid${
        poster ? ' discovery-grid--poster' : ''
      }${representative ? ' discovery-grid--representative' : ''}`}
    >
      {DISCOVERY_CARDS.map((card, i) => (
        <article key={card.id} className={`discovery-tile discovery-tile-${i + 1}`}>
          <span className="discovery-num">{card.icon}</span>
          <h4>{card.title}</h4>
          <p>{discoveryDetail(card.id, m)}</p>
        </article>
      ))}
      {poster ? (
        <MassHighlightPanel m={m} />
      ) : representative ? null : (
        <figure className="discovery-mass">
          <img src="/figures/task5_mass_pie.png" alt="体积与质量占比" loading="lazy" />
          <figcaption>
            少数致密区：体积 {m.tailAbovePct.toFixed(2)}% · 质量 {m.massAbovePct.toFixed(1)}%
          </figcaption>
        </figure>
      )}
    </div>
  );
```

**输入 / 输出**：输入 timeline、variant? → 发现网格 + 质量占比图。

**答辩 30 秒**：长卷第五节把四个结论写成可读卡片，poster 变体用 CSS donut 替代静态饼图。

## SparklineTriplet.tsx

**路径**：[`src/dashboard/SparklineTriplet.tsx`](../../../src/dashboard/SparklineTriplet.tsx)

**职责**：三条 SVG 迷你折线：σ(t)、≥p99 体积、≤p01 体积百步走势。

**为什么选这段**：展示 sparkline 绘制。

```47:83:src/dashboard/SparklineTriplet.tsx
export function SparklineTriplet({ timeline, metrics }: SparklineTripletProps) {
  const steps = timeline.timesteps;
  const std = steps.map((s) => s.std);
  const tailAbove = steps.map((s) => s.tailMassAboveP99 * 100);
  const tailBelow = steps.map((s) => s.tailMassBelowP01 * 100);

  const tailAboveRatio =
    tailAbove[0]! > 0
      ? `+${(((tailAbove[99]! - tailAbove[0]!) / tailAbove[0]!) * 100).toFixed(0)}%`
      : '—';
  const tailBelowRatio =
    tailBelow[0]! > 0
      ? `+${(((tailBelow[99]! - tailBelow[0]!) / tailBelow[0]!) * 100).toFixed(0)}%`
      : '—';

  return (
    <div className="spark-triplet">
      <Sparkline
        label="标准差 σ"
        values={std}
        color="#3dd6c6"
        badge={`+${metrics.sigmaPct.toFixed(1)}%`}
      />
      <Sparkline
        label="≥p99 体积占比"
        values={tailAbove}
        color="#f5a623"
        badge={tailAboveRatio}
      />
      <Sparkline
        label="≤p01 体积占比"
        values={tailBelow}
        color="#58a6ff"
        badge={tailBelowRatio}
      />
    </div>
  );
```

**输入 / 输出**：输入 timeline、metrics → 三条 spark-card。

**答辩 30 秒**：不用打开大图也能看到百步趋势形态，badge 显示相对 t=0 增幅百分比。

## PhaseTrack.tsx

**路径**：[`src/dashboard/PhaseTrack.tsx`](../../../src/dashboard/PhaseTrack.tsx)

**职责**：三段演化阶段轨道：线性期 / 非线性增长期 / 宇宙网形成期，高亮当前阶段。

**为什么选这段**：展示阶段轨道 UI。

```7:28:src/dashboard/PhaseTrack.tsx
export function PhaseTrack({ phaseId }: PhaseTrackProps) {
  return (
    <div className="phase-track" role="presentation">
      <div className="phase-track-bar">
        <span className={`phase-seg${phaseId === 'linear' ? ' active' : ''}`}>
          早期宇宙 · 线性期
        </span>
        <span className={`phase-seg${phaseId === 'nonlinear' ? ' active' : ''}`}>
          非线性增长期
        </span>
        <span className={`phase-seg${phaseId === 'web' ? ' active' : ''}`}>
          宇宙网形成期
        </span>
      </div>
      <div className="phase-track-labels">
        <span>t=0</span>
        <span>t=29</span>
        <span>t=69</span>
        <span>t=99</span>
      </div>
    </div>
  );
```

**输入 / 输出**：输入 phaseId → 三段 phase-seg 高亮条。

**答辩 30 秒**：与 VideoDashboardHeader 的 phase pill 同一套三阶段叙事，长卷用横向轨道强化时间结构。

## PosterFlowchart.tsx

**路径**：[`src/dashboard/PosterFlowchart.tsx`](../../../src/dashboard/PosterFlowchart.tsx)

**职责**：七步分析流程图：数据→体渲染→统计→刷选→投影→验证→发现。

**为什么选这段**：展示流程节点定义。

```97:125:src/dashboard/PosterFlowchart.tsx
export function PosterFlowchart() {
  return (
    <div className="pl-flow" role="list" aria-label="分析流程：从涨落到宇宙网">
      {STEPS.map((step, i) => (
        <div key={step.num} className="pl-flow-segment" role="presentation">
          <article className="pl-flow-node" role="listitem">
            <span className="pl-flow-num">{step.num}</span>
            <span className="pl-flow-icon">{step.icon}</span>
            <h3 className="pl-flow-title">{step.title}</h3>
            <p className="pl-flow-sub">{step.sub}</p>
          </article>
          {i < STEPS.length - 1 ? (
            <div className="pl-flow-arrow" aria-hidden>
              <svg viewBox="0 0 48 24" className="pl-flow-arrow-svg">
                <path
                  d="M4 12 H36 M30 6 L38 12 L30 18"
                  fill="none"
                  stroke="currentColor"
                  strokeWidth="2.5"
                  strokeLinecap="round"
                  strokeLinejoin="round"
                />
              </svg>
            </div>
          ) : null}
        </div>
      ))}
    </div>
  );
```

**输入 / 输出**：无 props → 七节点横向流程图。

**答辩 30 秒**：答辩收尾指第六节流程图：强调 pipeline 完整性，从 Nyx dat 到科学结论可复现。

## CosmicPosterLayout.tsx

**路径**：[`src/dashboard/CosmicPosterLayout.tsx`](../../../src/dashboard/CosmicPosterLayout.tsx)

**职责**：app 长卷六节滚动布局：hero、演化缩略图、统计、刷选验证、发现卡、流程图。

**为什么选这段**：展示长卷 section 骨架。

```42:94:src/dashboard/CosmicPosterLayout.tsx
  return (
    <div className="poster-layout">
      <section id="story-01" className="pl-section pl-s01">
        <InfographicHeader
          num="01"
          title="宇宙网诞生记"
          subtitle="基于 Nyx 模拟的 128³ 气体密度数据，揭示宇宙大尺度结构（宇宙网）的形成过程"
        />
        <div className="pl-s01-split">
          <aside className="pl-s01-left">
            <MetaStrip />
            <VerticalColorLegend min={dataMin} max={dataMax} />
            <dl className="pl-s01-stats">
              <div className="pl-s01-stat">
                <dt>当前步 σ</dt>
                <dd>
                  t={timestep} · {stepStats.std.toFixed(4)}
                </dd>
              </div>
              <div className="pl-s01-stat">
                <dt>p99 − p01</dt>
                <dd>+{metrics.spanPct.toFixed(1)}%</dd>
              </div>
            </dl>
          </aside>
          <div className="pl-hero-frame">
            {heroSlot ?? (
              <img
                src="/figures/task1_vol_t0099.png"
                alt="t=99 宇宙网体渲染"
                className="pl-hero-img"
                onError={(e) => {
                  e.currentTarget.src = '/figures/task1_slice_t0099.png';
                }}
              />
            )}
          </div>
        </div>
      </section>

      <section id="story-02" className="pl-section pl-s02">
        <InfographicHeader
          num="02"
          title="宇宙如何长大：100 步演化全景"
          subtitle="从均匀气体到纤维、节点分明的宇宙网拓扑"
        />
        <EvolutionThumbnails
          timeline={timeline}
          active={timestep}
          onSelect={onSelectTimestep}
        />
        <PhaseTrack phaseId={phase.id} />
      </section>
```

**输入 / 输出**：输入 timeline、timestep、dataMin/Max、heroSlot 等 → 完整 poster DOM。

**答辩 30 秒**：长卷一页讲完六个故事章节，每节有 InfographicHeader 和独立 id，可深链接滚动定位。

## InteractiveBrushLab.tsx

**路径**：[`src/dashboard/InteractiveBrushLab.tsx`](../../../src/dashboard/InteractiveBrushLab.tsx)

**职责**：探索页刷选实验台：直方图 + 体渲染 slot + XY DensityProjection 三列联动。

**为什么选这段**：展示三列 brush lab 网格。

```21:79:src/dashboard/InteractiveBrushLab.tsx
export function InteractiveBrushLab({
  timeline,
  densityData,
  volumeReady,
  loading,
  brushRange,
  domainMin,
  domainMax,
  onTop1,
  onBottom1,
  onFilament,
  onClear,
  vtkSlot,
}: InteractiveBrushLabProps) {
  const projection =
    densityData && volumeReady && !loading ? (
      <DensityProjection
        data={densityData}
        brushRange={brushRange}
        axis="xy"
        domainMin={domainMin}
        domainMax={domainMax}
        className="lab-projection"
      />
    ) : (
      <div className="lab-placeholder">加载体数据后显示投影…</div>
    );

  return (
    <div className="brush-lab">
      <div className="brush-lab-presets">
        <button type="button" className="lab-btn lab-top" onClick={onTop1}>
          Top 1% 节点
        </button>
        <button type="button" className="lab-btn lab-fil" onClick={onFilament}>
          90–99% 纤维
        </button>
        <button type="button" className="lab-btn lab-bot" onClick={onBottom1}>
          Bottom 1% 空洞
        </button>
        <button type="button" className="lab-btn lab-clear" onClick={onClear}>
          清除
        </button>
      </div>
      <div className="brush-lab-grid">
        <div className="brush-lab-cell">
          <span className="brush-lab-label">相空间直方图（拖拽框选）</span>
          <DensityHistogram timeline={timeline} />
        </div>
        <div className="brush-lab-cell">
          <span className="brush-lab-label">体渲染联动</span>
          <div className="brush-lab-vtk">{vtkSlot}</div>
        </div>
        <div className="brush-lab-cell">
          <span className="brush-lab-label">XY 最大密度投影</span>
          {projection}
        </div>
      </div>
    </div>
```

**输入 / 输出**：输入 timeline、densityData、brushRange、vtkSlot、预设回调 → 三列实验台。

**答辩 30 秒**：任务四交互演示核心：一列拖直方图、一列看体渲染、一列看投影，三路共用 brushRange。

## BrushVerifySection.tsx

**路径**：[`src/dashboard/BrushVerifySection.tsx`](../../../src/dashboard/BrushVerifySection.tsx)

**职责**：长卷刷选验证：Top/Bottom 两行（统计图+投影+要点），中间嵌 live DensityHistogram。

**为什么选这段**：展示 VerifyRow 与双投影。

```115:168:src/dashboard/BrushVerifySection.tsx
  return (
    <div className="brush-verify">
      <div className="brush-presets story-presets">
        <button type="button" className="preset preset-top" onClick={onTop1}>
          Top 1% 节点
        </button>
        <button type="button" className="preset preset-filament" onClick={onFilament}>
          90–99% 纤维
        </button>
        <button type="button" className="preset preset-bottom" onClick={onBottom1}>
          Bottom 1% 空洞
        </button>
        <button type="button" className="preset preset-clear" onClick={onClear}>
          清除刷选
        </button>
      </div>

      <VerifyRow
        tone="top"
        title="Top 1% 高密度尾"
        bullets={topBullets}
        histFigure="/figures/task4_hist_brush_top1.png"
        projection={projection}
      />

      <div className="verify-hist-live">
        <h4>交互直方图（拖拽框选密度区间）</h4>
        <DensityHistogram timeline={timeline} />
      </div>

      <VerifyRow
        tone="bottom"
        title="Bottom 1% 低密度区"
        bullets={bottomBullets}
        histFigure="/figures/task4_hist_brush_bottom1.png"
        projection={
          densityData && volumeReady && !loading ? (
            <DensityProjection
              data={densityData}
              brushRange={
                stats
                  ? { min: stats.min, max: stats.p01 }
                  : brushRange
              }
              axis="xy"
              domainMin={domainMin}
              domainMax={domainMax}
              className="projection-verify"
            />
          ) : (
            <p className="panel-placeholder">加载体数据…</p>
          )
        }
      />
```

**输入 / 输出**：输入 timeline、densityData、brushRange、stats → 验证区 DOM。

**答辩 30 秒**：统计—空间闭环的静态叙事版：每行左边刷选示意、中间实时投影、右边文字要点，结论写在 verify-conclusion。


---

## 与第 10 章的衔接

| 10 章主题 | 本章对应 |
|-----------|----------|
| [§1 全局联动](./10-答辩代码讲解.md#1--全局联动答辩必讲) | VideoDashboard + useDashboardInteraction → 投影/刷选 |
| [§3 任务二演化](./10-答辩代码讲解.md#3--任务二宇宙密度演化规律) | VideoEvolutionPanel、VideoSpatialPanel |
| [§4 任务三直方图](./10-答辩代码讲解.md#4--任务三密度分布与时序统计) | VideoHistMethodStrip、DensityHistogram（右栏） |
| [§5 任务四刷选](./10-答辩代码讲解.md#5--任务四相空间刷选与空间验证) | VideoBrushPreviews、BandPreviewCanvas、BrushVerifySection |

相关子章：[10a 体渲染](./10a-体渲染与VTK代码.md) · [10b 直方图](./10b-直方图与D3代码.md) · [10c 刷选 Worker](./10c-刷选与Worker代码.md)
